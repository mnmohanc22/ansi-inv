- name: "1.6.2 | PATCH | Ensure system wide crypto policy disables sha1 hash and signature support"
  when:
    - "'NO-SHA1' not in rhel8cis_crypto_policy_module"
  block:
    - name: "1.6.2 | PATCH | Ensure system wide crypto policy disables sha1 hash and signature support"
      ansible.builtin.template:
        src: etc/crypto-policies/policies/modules/NO-SHA1.pmod.j2
        dest: /etc/crypto-policies/policies/modules/NO-SHA1.pmod
        owner: root
        group: root
        mode: 'u-x,g-wx,o-rwx'
      register: discovered_no_sha1_template

    - name: "1.6.2 | PATCH | Ensure system wide crypto policy disables sha1 hash and signature support | submodule to crypto policy modules"
      ansible.builtin.set_fact:
        rhel8cis_crypto_policy_module: "{{ rhel8cis_crypto_policy_module + ':' + 'NO-SHA1' }}"
      changed_when: discovered_no_sha1_template is changed # noqa: no-handler
      notify:
        #- Update_Crypto_Policy
        - Set_Crypto_Policy

  



- name: "1.6.3 | PATCH | Ensure system wide crypto policy disables cbc for ssh"
  when:
    - "'NO-SSHCBC' not in rhel8cis_crypto_policy_module"
  block:
    - name: "1.6.3 | PATCH | Ensure system wide crypto policy disables cbc for ssh | Add submodule exclusion"
      ansible.builtin.template:
        src: etc/crypto-policies/policies/modules/NO-SSHCBC.pmod.j2
        dest: /etc/crypto-policies/policies/modules/NO-SSHCBC.pmod
        owner: root
        group: root
        mode: 'u-x,g-wx,o-rwx'
      register: discovered_no_sshcbc_template

    - name: "1.6.3 | PATCH | Ensure system wide crypto policy disables cbc for ssh | submodule to crypto policy modules"
      ansible.builtin.set_fact:
        rhel8cis_crypto_policy_module: "{{ rhel8cis_crypto_policy_module + ':' + 'NO-SSHCBC' }}"
      changed_when: discovered_no_sshcbc_template is changed # noqa: no-handler
      notify:
       # - Update_Crypto_Policy
        - Set_Crypto_Policy

  




### 1.6.3 Ensure system wide crypto policy macs are configured
- name: "1.6.4 | PATCH | Ensure system wide crypto policy macs are configured"
  when:
    - "'NO-WEAKMAC' not in rhel8cis_crypto_policy_module"
  block:
    - name: "1.6.4 | PATCH | Ensure system wide crypto policy macs are configured | Add submodule exclusion"
      ansible.builtin.template:
        src: etc/crypto-policies/policies/modules/NO-WEAKMAC.pmod.j2
        dest: /etc/crypto-policies/policies/modules/NO-WEAKMAC.pmod
        owner: root
        group: root
        mode: 'u-x,g-wx,o-rwx'
      register: discovered_ssh_macs_template

    - name: "Debug discovered_ssh_macs_template"
      ansible.builtin.debug:
        msg: "discovered_ssh_macs_template: {{ discovered_ssh_macs_template }}"

    - name : "Debug rhel8cis_crypto_policy_module before adding NO-WEAKMAC"
      ansible.builtin.debug:
        msg: "rhel8cis_crypto_policy_module: {{ rhel8cis_crypto_policy_module }}"  
       

    - name: "1.6.4 | PATCH | Ensure system wide crypto policy macs are configured | add submodule to crypto policy modules"
      ansible.builtin.set_fact:
        rhel8cis_crypto_policy_module: "{{ rhel8cis_crypto_policy_module + ':' + 'NO-WEAKMAC' }}"
      changed_when: discovered_ssh_macs_template is changed # noqa: no-handler
      notify:
       # - Update_Crypto_Policy
        - Set_Crypto_Policy

    # - name : "Debug rhel8cis_crypto_policy_module after adding NO-WEAKMAC"
    #   ansible.builtin.debug:
    #     msg: "rhel8cis_crypto_policy_module: {{ rhel8cis_crypto_policy_module }}" 
    #   changed_when: discovered_ssh_macs_template is changed

      #changed_when: discovered_ssh_macs_template is changed

    # - name: "1.6.4 | PATCH | Ensure system wide crypto policy macs are configured | submodule to crypto policy modules"
    #   ansible.builtin.set_fact:
    #     rhel8cis_crypto_policy_module: "{{ rhel8cis_crypto_policy_module + ':' + 'NO-WEAKMAC' }}"
    #   changed_when: discovered_ssh_macs_template is changed # noqa: no-handler
    #   notify:
    #    # - Update_Crypto_Policy
    #     - Set_Crypto_Policy




#### 1.6.5 Ensure system wide crypto policy disables chacha20-poly1305 for ssh
- name: "1.6.5 | PATCH | Ensure system wide crypto policy disables chacha20-poly1305 for ssh"
  when:
    - "'NO-SSHCHACHA20' not in rhel8cis_crypto_policy_module"
  block:
    - name: "1.6.5 | PATCH | Ensure system wide crypto policy disables chacha20-poly1305 for ssh | Add submodule exclusion"
      ansible.builtin.template:
        src: etc/crypto-policies/policies/modules/NO-SSHCHACHA20.pmod.j2
        dest: /etc/crypto-policies/policies/modules/NO-SSHCHACHA20.pmod
        owner: root
        group: root
        mode: 'u-x,g-wx,o-rwx'
      register: discovered_no_sshchacha20_template

    - name: "1.6.5 | PATCH | Ensure system wide crypto policy disables chacha20-poly1305 for ssh | submodule to crypto policy modules"
      ansible.builtin.set_fact:
        rhel8cis_crypto_policy_module: "{{ rhel8cis_crypto_policy_module + ':' + 'NO-SSHCHACHA20' }}"
      changed_when: discovered_no_sshchacha20_template is changed # noqa: no-handler
      notify:
       # - Update_Crypto_Policy
        - Set_Crypto_Policy